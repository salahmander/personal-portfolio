import { ReactNode } from "react";

export type NavLinksType = {
  title: string;
  icon?: ReactNode;
  url: string;
};

export type SocialIconsType = {
  icon: ReactNode;
  url: string;
  title: string;
};
